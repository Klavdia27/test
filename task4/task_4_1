#!/bin/bash
# код языка
# вывод в командную строку сообщения
echo Введите значение переменной a 
# запись ввода из командной строки в переменную а
read a
var=1
for ((var=1; var<=a; var++))
do
      printf $var" "
done

